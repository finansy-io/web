import {APP_TEXT} from '@shared/constants';

export const statusDuration = 1500;

export const statusManualCloseDelay = 300;

export const STATUS_POPUP_TEXT = {
	...APP_TEXT.statusPopupText,
} as const;
